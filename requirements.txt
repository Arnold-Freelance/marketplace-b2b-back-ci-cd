aiofiles
pillow
jinja2
pydantic-settings
# API + serveur
fastapi>=0.110,<0.116
uvicorn[standard]>=0.23,<0.31
starlette==0.46.2
python-multipart>= 0.0.20

# Modèle de données / ORM
SQLAlchemy>=2.0,<2.1

# Driver PostgreSQL (sync, simple, évite la compile sous Windows)
psycopg2-binary>=2.9,<2.10

# Schémas / validation — Pydantic v2 (migre Phase 4 code-facto)
pydantic>=2.6,<3.0
email-validator>=2.0,<3.0   # requis par EmailStr

# JWT (ton `import jwt` vient de PyJWT)
PyJWT>=2.8,<3.0

# Hashing des mots de passe (bcrypt utilisé directement — passlib 1.7.4 est
# abandonné et casse à chaque montée de version de bcrypt : cf. 500 register)
bcrypt>=4.1.2,<5.0

# Migrations DB
alembic>=1.13,<2.0

# Tests (Phase 5 code-facto)
pytest>=8.0,<9.0
httpx>=0.27,<1.0      # TestClient FastAPI
pytest-asyncio>=0.23,<1.0